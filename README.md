# core-runtime-orchestrator
CORE Runtime Orchestrator — validates engine manifests/schemas, enforces CORE-only coupling, strips forbidden context, canonicalizes JSON (RFC 8785 JCS), seals sha256 Run Bundles, and supports deterministic replay/audit.
